pub mod hint_system {
    //Hint: pops up with a hint whenever either a condition is met or if they're turned on, we'll decide that later (UNFINISHED)
    pub struct Hint {
        
    }

    impl Hint {
        
    }
}